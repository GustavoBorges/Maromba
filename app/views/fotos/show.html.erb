<p id="notice"><%= notice %></p>

<%= image_tag @foto.image_url(:thumb).to_s %>

<p>
  <strong>Data:</strong>
  <%= @foto.data %>
</p>

<p>
  <strong>Descricao:</strong>
  <%= @foto.descricao %>
</p>

<%= link_to 'Edit', edit_foto_path(@foto) %> |
<%= link_to 'Back', fotos_path %>
